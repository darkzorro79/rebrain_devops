[Unit]
Description=Todo Backend Service
After=network.target postgresql.service

[Service]
Type=simple
User=back
WorkingDirectory={{ backend_dir }}
Environment=LISTEN_ADDR=127.0.0.1:8080
ExecStart={{ backend_dir }}/back
Restart=always

[Install]
WantedBy=multi-user.target 